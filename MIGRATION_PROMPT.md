# 防災アプリ実装依頼プロンプト

以下の要件を満たす、実際に起動できる防災Webアプリを一度の実装で作成してください。
実装後は、必要な依存関係を設定し、ローカルで起動して主要URLを確認してください。
既存プロジェクトがある場合は、その構成や利用中の技術に合わせて実装し、無関係な機能は変更しないでください。

## 技術要件

- Python 3 と Flask を使用する
- HTMLテンプレートは共通レイアウトを継承する
- 日本語UI、レスポンシブ対応にする
- 避難所と指示データはJSONファイルに保存し、アプリ起動時に読み込む
- 保存先ファイルが存在しない、または壊れている場合は空データとして扱う
- Vercelでデプロイ可能な構成にする
- ページ表示とAPIのエラーが、可能な範囲で画面を壊さないようにする

## アプリの目的

青森県青森市の住民が、気象警報・注意報、避難指示・情報、避難所情報を確認できる防災アプリにする。
画面上部には「防災アプリ」のヘッダーとナビゲーションを配置する。

ナビゲーションには次のリンクを置く。

- ホーム `/`
- 避難所検索 `/shelter_search`
- 避難所登録 `/shelter_register`
- 指示・発信ボード `/board`
- ログイン `/login` またはログアウト `/logout`

ログイン状態に応じて「ログイン中」とユーザー名、または「未ログイン」を表示する。

## ホーム画面 `/`

次の内容を上から順に表示する。

1. 気象警報・注意報セクション
2. 青森市役所のお問い合わせ先
3. 住民向けアクションカード
4. 青森市中心の避難所マップ

### 気象警報・注意報

- 初期表示は「読み込み中...」にする
- ブラウザから `/api/weather_warnings` を取得して表示する
- 対象は青森県コード `020000`、青森市コード `0220100`
- 気象庁の新形式JSONから、対象地域の `class20Items` と `kinds` を探す
- `status` が「発表」または「継続」の情報だけを表示する
- 同じ警報コードは重複表示しない
- 発表時刻は日本時間に変換して表示する
- 警報がある場合は警告色の表示と警戒レベルのバッジを表示する
- 警報がない場合は「現在、警報・注意報は発表されていません」と表示する
- 取得失敗時は「気象情報の取得に失敗しました」と表示する
- ページ読み込み時に取得し、一定間隔で自動更新する

警報コードは少なくとも、暴風雪、大雨、洪水、暴風、大雪、波浪、高潮、土砂災害、雷、強風、濃霧、乾燥、低温などを日本語名称へ変換できるようにする。未知のコードはコード番号付きで表示する。

### お問い合わせ先

- ホーム画面の警報セクション直下に「お問い合わせ先」として表示する
- 連絡先名は「青森県青森市役所」
- 電話番号は `017-734-1111`
- 電話番号はスマートフォンから発信できる `tel:0177341111` リンクにする

### アクションカード

次の3枚を表示する。

- 「避難指示・情報」: 危険な場所から安全な場所に避難する案内。ボタン「詳細を見る」は `/shelter_search` に遷移させる
- 「安否確認」: ボタン「登録済み」で `/safety_confirmation` に遷移させる
- 「情報・注意事項」: ボタン「確認する」で `/board` に遷移させる

### 避難所マップ

- LeafletとOpenStreetMapを使用し、青森市付近 `[40.8269, 140.7444]` を中心に表示する
- マップ本体はホーム画面に残す
- 避難所のピン、マーカー、ポップアップは表示しない
- 避難所APIの取得や `L.marker` の生成をホーム画面のJavaScriptに実装しない

## 避難所検索

### `/shelter_search`

登録済み避難所を確認できる画面にする。「全施設一覧」から `/all_shelters` へ移動できる。避難所名と現在地住所の文字検索欄、チェックボックス絞り込み、並び替え、赤色のリセットボタンを配置する。検索画面のヒット数の右横に「避難所一覧」ボタンを置き、`/all_shelters` へ遷移させる。住所欄は既存の住所文字列検索として扱い、ジオコーディングは追加しない。

### `/all_shelters` と `/search_results`

- 避難所名を一覧表示する
- データが空の場合は「該当する避難所が見つかりませんでした」と表示する
- 検索画面とトップページへ戻るリンクを置く
- `/search_results?district=地区名` のように地区で絞り込めるようにする

### 避難所データ

各避難所は少なくとも次の項目を持てるようにする。

```json
{
  "id": 1,
  "name": "避難所名",
  "address": "住所",
  "phone": "電話番号",
  "capacity": "収容可能人数",
  "district": "地区名"
}
```

## 避難所登録 `/shelter_register`

- 管理者ログイン必須にする
- 避難所名を必須入力にする
- 住所、電話番号、収容可能人数を入力できるようにする
- 既存IDの最大値に1を加えて新しいIDを作る
- 登録後はJSONファイルへ保存する
- 成功時と失敗時に日本語メッセージを表示する
- 入力値は空欄を適切に扱う

## 指示・発信ボード `/board`

- 管理者ログイン必須にする
- 住民向けの指示だけを一覧表示する
- 指示本文を表示する
- 指示に避難先がある場合は「避難先: 避難所名」も表示する
- 指示データはJSONファイルから読み込む

指示データの例:

```json
{
  "target": "住民",
  "content": "安全な場所へ避難してください",
  "shelter": "避難所名"
}
```

## 家族の安否確認 `/safety_confirmation`

- ホーム画面の「安否確認」カードから遷移できるようにする
- あらかじめ登録した家族などを一覧表示する
- 各人の名前、続柄、安否状態（「無事」「避難中」「未確認」）を表示する
- 各人の安否状態を選択して更新できるようにする
- 安否確認データはJSONファイルに保存し、更新後も保持する
- 登録者がいない場合は「登録されている家族はいません」と表示する

## ログインとセキュリティ

- `/login` はパスワード入力画面を表示する
- 認証成功後は、正当な同一ホスト内の `next` URLへ戻す
- `next` に外部ホストを指定してリダイレクトできないようにする
- 未ログインで `/shelter_register` または `/board` にアクセスした場合はログイン画面へリダイレクトする
- ログアウト時はセッションを破棄してホームへ戻す
- サンプル環境の管理者アカウントはユーザー名 `admin`、パスワード `123` とする
- 本番運用では秘密情報を環境変数へ移し、固定の秘密鍵やパスワードを残さない構成にできるようにする

## API

次のAPIを実装する。

- `GET /api/weather_warnings`: 対象地域の警報情報をJSONで返す
- `GET /api/shelters`: 全避難所をJSON配列で返す
- `GET /shelters?district=地区名`: 避難所をJSONで返し、該当なしは404とエラーJSONを返す

## 画面デザイン

- 共通ヘッダー、ナビゲーション、メイン領域を持つ
- 防災用途として見やすく、警報は赤系、通常情報は青・緑系など意味のある色を使う
- カード、ボタン、警報表示、マップの幅と高さをレスポンシブにする
- モバイル画面でも文字やボタンが重ならないようにする
- ホーム画面のマップは高さ400px程度を確保する

## 受け入れ条件

以下をすべて確認してから完了としてください。

- `GET /` が200を返し、ホーム画面が表示される
- ホーム画面に「青森県青森市役所」と `017-734-1111` が表示される
- お問い合わせ先の電話番号をタップすると `tel:0177341111` で発信できる
- ホーム画面の「避難指示・情報」の「詳細を見る」が `/shelter_search` を指す
- ホーム画面の「安否確認」の「登録済み」が `/safety_confirmation` を指す
- `/safety_confirmation` で登録済み家族の安否を確認・更新できる
- ホーム画面にLeafletの地図はあるが、避難所ピンは生成されない
- `GET /shelter_search` と `GET /all_shelters` が表示できる
- 未ログインで保護ページへ行くと `/login` へ遷移する
- パスワード `123` でログイン後、避難所登録と指示ボードへアクセスできる
- 空の避難所名では登録できない
- 避難所登録後にJSONへ保存され、一覧と `/api/shelters` に反映される
- `/api/weather_warnings` が外部API障害時にもJSONを返す
- Pythonの構文チェック、JSONの構文チェック、主要URLの簡易テストを実行する

実装完了時には、作成・変更したファイル、起動方法、確認したテスト結果を簡潔に報告してください。